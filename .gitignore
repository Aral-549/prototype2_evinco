# Virtual environments
.venv/
venv/
ENV/
env/

# Python cache & compiled bytecode
__pycache__/
*.py[cod]
*$py.class
*.so
.pytest_cache/
.coverage
htmlcov/

# Operating System temporary files
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# IDE & Editor files
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# Environment variables
.env
.env.local

# The raw MoSPI harvest is deliberately NOT ignored. Every performance figure
# the platform reports is derived from it via scripts/build_panel.py, and the
# README promises end-to-end reproduction, so excluding it would make that
# promise false. ~12.6 MB.
# data/paimana_live_raw.json

# Python/pytest scratch
.pytest_cache/

# TypeScript incremental build cache
hackathon-frontend-starter/tsconfig.tsbuildinfo
